# Copy to .env and add your real keys (never commit .env)
# Supabase: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Gemini key for Edge Function: set in Supabase Dashboard → Edge Functions → analysis_trigger → Secrets as GEMINI_API_KEY
# Optional for local: VITE_GEMINI_API_KEY (used only if you call Gemini from frontend)
